# DeepSeek Monitor 项目结构

> 本文档描述 DeepSeek Monitor 主项目的目录结构与核心模块。根目录下的演示/子项目（`portfolio/`、`dev-explorer-ide/`、`deepseek-monitor/`、`deepseek-monitor-dashboard/`、`file01/`、`test_minimal/`、`visual-demo/`）不在本文档范围内。

## 项目简介

DeepSeek Monitor 是一个基于 Electron 的桌面悬浮窗应用，用于实时监控 DeepSeek API 的余额与调用量。它通过本地代理服务器拦截 API 调用、聚合用量数据，并在桌面透明悬浮窗中以图表形式展示余额、费用曲线、模型用量、Token 消耗等指标。

核心特性：

- 实时余额查询与用量统计
- 本地代理拦截 API 调用并聚合 Token / 费用
- 透明置顶悬浮窗，支持拖拽排序、字体缩放、暗色模式
- Window Physics Engine：物理化的窗口调整与布局重排
- 加密存储 API Key，支持自启动、托盘菜单、设置面板

## 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Electron | ^40.0.0 (开发依赖) / 28 (运行时基线) | 跨平台桌面应用框架 |
| ECharts | ^5.5.0 | 数据可视化图表 |
| electron-store | ^8.1.0 | 持久化设置存储 |
| gridstack | ^12.3.3 | 网格化布局 |
| electron-builder | ^24.9.0 | 应用打包 |
| Node.js | >= 18 (推荐 20+) | 运行环境 |

## 目录结构树

```
deepseek_monitor/
├── src/                          # 源代码主目录
│   ├── main/                     # Electron 主进程
│   │   ├── index.js              # 应用入口，窗口与托盘管理
│   │   ├── aggregator.js         # 用量数据聚合器
│   │   ├── balance.js            # API 余额查询
│   │   ├── fetcher.js            # 平台用量数据抓取
│   │   ├── pricing.js            # 模型计费表
│   │   ├── proxy.js              # 本地代理服务器
│   │   └── store.js              # 加密持久化存储
│   ├── preload/
│   │   └── preload.js            # 预加载脚本（IPC 白名单桥接）
│   └── renderer/                 # 渲染进程
│       ├── index.html            # 主悬浮窗入口
│       ├── login.html            # 登录窗口
│       ├── settings-window.html  # 设置窗口
│       ├── assets/               # 渲染进程静态资源
│       │   ├── icon.png
│       │   └── tray-icon.png
│       ├── css/
│       │   ├── layout.css        # 布局样式
│       │   ├── components.css    # 组件样式
│       │   └── main.css          # 全局基础样式
│       └── js/
│           ├── app.js            # 主窗口应用逻辑
│           ├── charts.js         # ECharts 图表封装
│           ├── login.js          # 登录窗口逻辑
│           ├── settings-definitions.js  # 设置项定义
│           ├── settings-window.js        # 设置窗口逻辑
│           ├── components/       # UI 组件
│           │   ├── cost-line.js
│           │   ├── curve-chart.js
│           │   ├── drag-sort.js
│           │   ├── fee-cards.js
│           │   ├── model-bar.js
│           │   └── token-line.js
│           ├── layout/           # 布局系统
│           │   ├── component-registry.js
│           │   ├── layout-controller.js
│           │   ├── layout-policy.js
│           │   └── reflow-animator.js
│           └── runtime/          # Window Physics Engine
│               ├── runtime.js
│               ├── physics.js
│               ├── constraint.js
│               ├── commit-manager.js
│               ├── window-state.js
│               └── debug-overlay.js
├── test/                         # 单元测试
│   ├── component-registry.test.js
│   ├── dashboard-markup.test.js
│   ├── layout-controller-static.test.js
│   ├── layout-policy.test.js
│   ├── reflow-animator.test.js
│   ├── settings-sync-static.test.js
│   └── window-runtime-static.test.js
├── docs/                         # 文档目录
│   └── superpowers/
│       ├── specs/                # 设计规格文档
│       └── plans/                # 实现计划文档
├── assets/                       # 应用级静态资源
│   └── icon.ico                  # 应用图标（Windows）
├── package.json                  # 项目依赖与脚本
├── electron-builder.yml          # 打包配置
├── .gitignore
└── 使用说明.md                    # 用户使用说明
```

## 核心模块详解

### `src/main/` — 主进程

主进程负责窗口管理、数据采集、代理服务与持久化。所有 Node.js 权限敏感操作均在此完成。

| 文件 | 职责 |
|------|------|
| `index.js` | 应用入口。创建并管理主悬浮窗、登录窗、会话窗、设置窗、系统托盘；调度余额与用量定时刷新；处理单实例锁、窗口边界持久化、IPC 通道注册 |
| `aggregator.js` | 用量聚合器。维护按日统计（模型维度：Token、费用、缓存命中/未命中、Prompt Token）与 2880 槽环形缓冲区，支持历史数据加载与落盘 |
| `balance.js` | 调用 `api.deepseek.com/user/balance` 查询账户余额（IS_AVAILABLE、CURRENCY、TOTAL、USED） |
| `fetcher.js` | 通过 `sessionToken` 从 `platform.deepseek.com` 抓取平台用量明细（费用与调用量） |
| `pricing.js` | 内置模型计费表（`deepseek-v4-pro`、`deepseek-v4-flash`、`deepseek-reasoner` 的 input/output/cache_hit 单价），提供 `calcCost` 计费函数 |
| `proxy.js` | 本地 HTTP 代理服务器。拦截经过的 DeepSeek API 请求，解析响应中的 usage 字段并喂给 Aggregator 聚合，同时上报代理运行状态 |
| `store.js` | 基于 electron-store 的加密存储。从 `userData/.key` 读取/生成 AES 密钥，加密保存 API Key 等敏感配置；管理窗口边界、主题、自启动、组件排序等设置 |

### `src/preload/preload.js` — 预加载脚本

通过 `contextBridge.exposeInMainWorld('api', ...)` 向渲染进程暴露受控的 IPC 接口，所有通道均经白名单校验：

- `on(channel, callback)` — 监听主进程事件（`settings:loaded`、`login:error`、`open:settings`、`theme:changed`、`window:bounds-changed`）
- `send(channel, data)` — 向主进程发送消息（设置更新、登录提交、窗口控制、刷新、resize 生命周期等）
- `invoke(channel, ...args)` — 调用主进程并取得返回值（`get:settings`、`get:dashboard` 等）

### `src/renderer/` — 渲染进程

#### HTML 入口

- `index.html` — 主悬浮窗，承载用量看板
- `login.html` — API Key 登录窗口
- `settings-window.html` — 设置面板窗口

#### CSS

- `main.css` — 全局基础样式与 CSS 变量（含 `--ui-font-scale` 字体缩放变量）
- `layout.css` — 布局相关样式
- `components.css` — 组件级样式

#### JS

**顶层脚本**

| 文件 | 职责 |
|------|------|
| `app.js` | 主窗口应用逻辑。WebSocket 数据刷新调度、图表 resize 节流、字体缩放控制、布局锁定状态管理 |
| `charts.js` | ECharts 图表封装，提供曲线图、柱状图等可视化构造 |
| `login.js` | 登录窗口交互逻辑，提交 API Key |
| `settings-definitions.js` | 设置项声明式定义（透明度、置顶、暗色模式、自启动、组件排序、字体缩放等） |
| `settings-window.js` | 设置窗口的渲染与同步逻辑 |

**`components/` — UI 组件**

每个组件对应悬浮窗中的一个可视化单元：

| 文件 | 职责 |
|------|------|
| `fee-cards.js` | 费用卡片展示 |
| `curve-chart.js` | 用量曲线图 |
| `cost-line.js` | 费用趋势线 |
| `token-line.js` | Token 消耗线图 |
| `model-bar.js` | 模型用量条形图 |
| `drag-sort.js` | 组件拖拽排序交互 |

**`layout/` — 布局系统**

| 文件 | 职责 |
|------|------|
| `component-registry.js` | 组件注册表，维护组件清单与运行时实例 |
| `layout-controller.js` | 布局控制器，协调组件排布与尺寸分配 |
| `layout-policy.js` | 布局策略，定义不同尺寸下的布局规则 |
| `reflow-animator.js` | 重排动画器，负责布局变化的过渡动画 |

**`runtime/` — Window Physics Engine**

物理化的窗口运行时系统，提供约束求解与物理化的窗口调整体验：

| 文件 | 职责 |
|------|------|
| `runtime.js` | 运行时主控，统一调度物理与约束 |
| `physics.js` | 物理模拟（惯性、阻尼等） |
| `constraint.js` | 约束系统，定义窗口尺寸/位置约束 |
| `commit-manager.js` | 状态提交管理器，处理布局变更的提交与回滚 |
| `window-state.js` | 窗口状态快照与持久化 |
| `debug-overlay.js` | 调试覆盖层，可视化物理与约束状态 |

## 测试目录 `test/`

基于 Node.js 内置 `node --test` 运行器的静态测试，聚焦布局系统与运行时的可单测逻辑：

| 文件 | 覆盖对象 |
|------|----------|
| `component-registry.test.js` | 组件注册表 |
| `dashboard-markup.test.js` | 看板 HTML 结构 |
| `layout-controller-static.test.js` | 布局控制器（静态） |
| `layout-policy.test.js` | 布局策略 |
| `reflow-animator.test.js` | 重排动画器 |
| `settings-sync-static.test.js` | 设置同步逻辑（静态） |
| `window-runtime-static.test.js` | 窗口运行时（静态） |

运行方式：`npm test`

## 文档目录 `docs/`

```
docs/superpowers/
├── specs/    # 设计规格文档（design）
└── plans/    # 实现计划文档（plan）
```

收录了如 `deepseek-monitor-design`、`dashboard-layout-design`、`ui-physics-engine-phase4`、`key-migration-fix` 等规格与计划文档，记录项目的演进决策。

## 构建与配置

### `package.json`

- **入口**：`src/main/index.js`
- **脚本**：
  - `npm start` — 启动 Electron 应用
  - `npm test` — 运行测试
  - `npm run build:win` — 构建 Windows 安装包
  - `npm run build:mac` — 构建 macOS 安装包
- **运行时依赖**：`echarts`、`electron-store`、`gridstack`
- **开发依赖**：`electron`、`electron-builder`

### `electron-builder.yml`

- **appId**：`com.deepseek.monitor`
- **productName**：`DeepSeek Monitor`
- **输出目录**：`build/`
- **打包文件**：`src/**/*`、`node_modules/**/*`、`package.json`
- **Windows**：NSIS 安装器，图标 `assets/icon.ico`
- **macOS**：DMG，图标 `assets/icon.icns`，支持自定义安装路径

### `.gitignore`

忽略 `node_modules/`、`build/`、`dist/`、`*.log`、`config.json`、`history.json`、`*.key`（密钥文件不入库）。

## 架构与数据流

```
┌──────────────────────────────────────────────────────────────┐
│                        主进程 (src/main)                      │
│                                                              │
│  index.js ── 窗口/托盘/调度                                   │
│     │                                                        │
│     ├── balance.js ─────► api.deepseek.com (余额)             │
│     ├── fetcher.js ─────► platform.deepseek.com (用量明细)    │
│     ├── proxy.js ──────► 本地 HTTP 代理 (拦截 API 调用)        │
│     │        │                                               │
│     │        ▼                                               │
│     │   aggregator.js ◄── 环形缓冲 + 按日聚合 + 历史落盘       │
│     │        ▲                                               │
│     │        │  计费                                          │
│     │     pricing.js                                         │
│     │                                                        │
│     └── store.js ──────► userData/.key (AES 加密)             │
│                          electron-store (设置/窗口边界)        │
│                                                              │
└──────────────────────────────┬───────────────────────────────┘
                               │ IPC (白名单通道)
                               │
                  ┌────────────┴────────────┐
                  │  preload.js (contextBridge) │
                  └────────────┬────────────┘
                               │ window.api
                               │
┌──────────────────────────────┴───────────────────────────────┐
│                     渲染进程 (src/renderer)                    │
│                                                              │
│  app.js ── 主窗口主控                                        │
│     │                                                        │
│     ├── ComponentRegistry ──► components/ (fee-cards,        │
│     │                          curve-chart, model-bar ...)    │
│     │                                                        │
│     ├── LayoutController ───► layout/ (policy, animator)     │
│     │                                                        │
│     └── Runtime ───────────► runtime/ (physics, constraint,  │
│                               commit-manager, window-state)  │
│                                                              │
│  charts.js / login.js / settings-window.js                   │
└──────────────────────────────────────────────────────────────┘
```

**数据流向**：

1. 用户在登录窗口输入 API Key → 经 preload IPC 提交至主进程 → `store.js` 加密落盘
2. 主进程启动本地代理（`proxy.js`），应用通过该代理调用 DeepSeek API 时，代理解析响应 usage 并喂给 `aggregator.js`
3. `aggregator.js` 按模型/按日聚合 Token、费用、缓存命中，写入环形缓冲与历史文件
4. `balance.js` 与 `fetcher.js` 定时拉取余额与平台用量明细
5. 主进程通过 IPC 将聚合数据推送至渲染进程
6. 渲染进程 `app.js` 调度各组件（`components/`）刷新图表，`layout/` 与 `runtime/` 负责布局重排与物理化窗口调整
